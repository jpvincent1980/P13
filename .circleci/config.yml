version: 2.1

description: Workflow de build, test, conteneurisation et déploiement

display:
  source_url: https://github.com/jpvincent1980/P13

orbs:
  python: circleci/python@2.0.0

jobs:
  build:
    executor: python/default
    steps:
      - checkout
      - python/install-packages:
          pkg-manager: pip
          pip-dependency-file: requirements.txt

workflows:
  main:
    jobs:
      - build